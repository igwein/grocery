---
paths:
  - "**/*.py"
  - "**/*.pyi"
---
# Python Hooks

> This file extends [common/hooks.md](../common/hooks.md) with Python specific content.

## Notes

No auto-formatting or type-checking hooks are configured yet.
Prefer `logging` module over `print()` for production code, but `print()` is acceptable during development.
